CatamotoPancakeV2AutoLiquidity_execute
├── when caller is not authorized
│   └── it reverts
└── when caller is authorized
    ├── when sender is authorized to bypass execution
    │   └── it skips
    └── when sender is not authorized to bypass execution
        └── when token0 balance is not enough
            └── it skips