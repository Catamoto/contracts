CatamotoPancakeV2AutoLiquidity_constructor
├── when token0 is zero address
│   └── it reverts
├── when token1 is zero address
│   └── it reverts
├── when router is zero address
│   └── it reverts
├── when factory is zero address
│   └── it reverts
└── given token has been deployed
    └── it deploys token with given arguments